<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Beemessage" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_yellow</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary_yellow</item>
        <!-- Customize your theme here. -->
    </style>
</resources>